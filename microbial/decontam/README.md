# Readme 

### Installation R
To execute Rscript run_decontam.R, first make an separate environment in your current folder and export this library to user scope of dependencies. By the following:
```
mkdir .Rlib
export R_LIBS_USER=$(pwd)/.Rlib
# Make sure you have writing permission to this folder (by requesting privilages): /Library/Frameworks/R.framework/'
```

### Running R
Rscript run_decontam.R

### Output
Output could be found at `FBI/output/02_tables/03_intermediate/` for downstream analysis 
